# ensemble-llm-wiki — full dependency list
# Install: pip install -r requirements.txt

# Core
PyYAML>=6.0,<7.0
python-dateutil>=2.8.0
requests>=2.28.0

# PDF processing (text-based)
PyMuPDF>=1.23.0,<2.0

# PDF OCR (scanned documents)
pytesseract>=0.3.10,<1.0
Pillow>=10.0.0,<12.0
pdf2image>=1.16.0

# EPUB ebooks
EbookLib>=0.18,<1.0
beautifulsoup4>=4.12.0,<5.0
lxml>=4.9.0

# jieba is only needed for Chinese-language knowledge bases.
# English-only users can comment out this line.
jieba>=0.42,<1.0

# Knowledge graph generation (https://github.com/safishamsi/graphify)
# Installs networkx, scipy, datasketch, rapidfuzz as sub-dependencies
graphifyy>=0.8.0

# Semantic search / embedding (optional — improves kb_query ranking beyond TF-IDF)
# Without this, search falls back to pure keyword TF-IDF (still works well).
# fastembed>=0.3.0

# Skill Seekers integration (optional — for `kb fetch` command)
# Basic install:  pip install skill-seekers
# Full install:   pip install skill-seekers[all]
# skill-seekers>=3.5.0

# System dependencies (not installed by pip — install separately):
#   macOS:  brew install tesseract poppler
#   Linux:  sudo apt install tesseract-ocr poppler-utils
