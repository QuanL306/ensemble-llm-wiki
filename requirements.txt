# knowledge-base-suite-en — ensemble LLM wiki builder
# Install: pip install -r requirements.txt

# PDF document parsing (text-based)
PyMuPDF>=1.23.0

# PDF OCR (scanned documents)
pytesseract>=0.3
Pillow>=10.0

# EPUB ebooks
EbookLib>=0.18
beautifulsoup4>=4.12

# Chinese NLP (word segmentation + POS tagging for concept extraction)
# Only needed when building Chinese-language knowledge bases.
# English-only users can safely skip: pip install -r requirements.txt --ignore-requires-python
# or just comment this line out before installing.
jieba>=0.42

# Knowledge graph generation (https://github.com/safishamsi/graphify)
# Installs networkx, scipy, datasketch, rapidfuzz as sub-dependencies
graphifyy>=0.8.0
