# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
.claude/

# OS
.DS_Store
Thumbs.db

# Generated / runtime
*.log
.env
graphify-out/
wiki/graphify-out/
# raw/ is excluded here so that if someone accidentally runs 'kb init .'
# inside the cloned repo, their source documents don't get tracked.
# Knowledge bases should be initialized outside this repo (e.g. ~/my-research).
raw/

# Private dev files — keep local, never publish
docs/DEV_LOG.md
docs/AUDIT.md
